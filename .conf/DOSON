1
# To enable ninja change the 1 to a 0.
# Change the first line of this file to 0 (true) to enable ninja to build the `*.so` files instead of make for the APK build.  This feature is under development.
# DOSON EOF
